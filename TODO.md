# TODO: Pending tasks

Improve the console logging: silent, regular and debugging.

---

Implement Pagefind (<https://pagefind.app/>) in the HTR and use the created index as the resindex.

Create the cipr.info HTR with the Specification and a friendly guide to get indexed in the Cipr.

Implement `QUERY /za/` to search in the resindex, it mimics `QUERY /` (ranking, filtering, pagination, ordering, tie-breaking, etc.).

Update Alps again

Improve the texts in the Help page.

Create page "Random Entries" in the ciprface to show randomly selected entries with filtering.

Create the A or CNAME record in the DNS during the first startup process, it implies asking the destination IP or domain in ciprnode.toml.

Implementation of the leech mode.

Add Ukrainian and Arabic languages

Implement dark mode.

---

The visual elements (shapes, lines, text, icons, etc.)
The layout and composition (where things should go)
Colors and styling (hex codes, gradients, stroke widths)
The intended dimensions or aspect ratio (e.g., 24x24 icon, or a 800x600 diagram)

---

Nice, I want to create a 3D isometric SVG as follows:

**Global Settings**:

- Dimensions: iy must looks sharp in a 4K screen and also in a tablet or big screen smartphone
- The overall background color is #fefefe, but ensure all elements are going to be visible when switching to a dark mode wit a #333333 background color, or similar.
- For any text, use the color #090909
- The animation should loop infinitely
- All visual elements must be minimalistic

**Scene #1 (0s to 3s)**:

The text "Cipr" appears in the center and just above the text "Cosmic Index of Public Resources" appears as it is being typed, letter by letter.

**Transition (3s to 5s)**:

Scene #1 completely fades out and Scene #2 fades in.

**Scene #2 (5s to 14s)**:

A figurative representation of the Internet is shown, it consist of a bunch of cubes with rounded corners in different colors, they are randomly jumping and moving around, up and down, back and forth, growing and shrinking, coming and going, disappearing and appearing again, interacting between them by exchanging tiny dots that represent their communication links.

The surface of each cube has a banner or icon about what it represent: Some are servers, routers and switches; others are protocols, languages and Internet systems (TCP/IP, FTP, UDP, SSH, HTTP, HTML, JS, CSS, DNS...); others represent ISPs, the W3C, the FSF, government agencies, cloud providers, social media providers, search engine providers, browsers, APIs, etc. You need to figure out how to fit in every cube an icon that tells what the cube represents.

Beneath all of the interacting cubes, in the bottom right of the scene, is an isolated cube, the Cipr cube, with only a #333333 colored circle (the Cipr logo) as its icon, this cube is not moving, nor shrinking, it is not communicating with the others, is there static, apart, partially covered in dust and cobwebs, abandoned, forgotten.

Since appearing, this whole scene have the following text as a legend in the bottom:

«The Cipr has existed since the inception of the Internet, but it was empty and overlooked until it was recently discovered by accident.»

**Scene #3 (14s to 20s)**:

Here the Scene #2 stays in place, nothing changes except that a tiny guy appears walking from left to right and accidentally stumbles upon the dusty Cipr cube. This tiny guy is Juan, he is a mulato person, all salt-and-pepper-haired, afro hairstyle, thick beard, black t-shirt, jeans and grey shoes.

**Transition (20s to 22s)**:

Everything fades out except Juan and the Cipr cube.

**Scene #4 (22s to 30s)**:

A laptop computer appears in front of Juan and he types on it during 4 seconds until a small sphere with the text `cn0` pops up from the laptop and stays floating over it, close to it.

The legend on the bottom on this scene says:

«The tiny guy realized that using the Cipr required a simple piece of software that didn't yet exist, so he programmed a proof of concept for it: the Ciprnode zero (cn0).»

**Scene #5 (30s to 45s)**:

In this scene the dust and cobwebs fade out and the Cipr cube moves to the center of the scene, and Juan walks to the left center of the scene with his computer and the `cn0` sphere moving relatively with him. Then, a dotted line grows from the sphere in direction to the Cipr cube to finally stablish a link between them. Once the link is in place, the Cipr cube start grows vertically a bit to become a cuboid (rectangular prism) in order to allow the insertion of a first unintelligible line of text under the Cipr logo, inside the cuboid (former cube). This new tiny line of text in the cuboid represents the first ever entry in the Cipr, and once it is in place, the following text pops from the tiny `cn0` sphere: "ciprnode.cipr.info".

Then a new tiny person pops, a woman holding a tiny `cn0` sphere in her hand, a dotted line grows from this new tiny `cn0` sphere to the cube, and the same happens as before, the cube grows a bit to allow the insertion of a new unintelligible line of text, and the following text pops from the tiny `cn0` sphere: "ciprnode.jane-blog.com".

And again, a new tiny person pops, an executive-looking man holding a tiny `cn0` sphere in his hand, a dotted line grows from this new tiny `cn0` sphere to the cube, and the same happens as before, the cube grows a bit more to allow the insertion of a new unintelligible line of text, and the following text pops from the tiny `cn0` sphere: "ciprnode.bill-business.com".

And again, a new tiny person pops, a young tattoo artist woman holding a tiny `cn0` sphere in her hand, a dotted line grows from this new tiny `cn0` sphere to the cube, and the same happens as before, the cube grows a bit to allow the insertion of a new unintelligible line of text, and the following text pops from the tiny `cn0` sphere: "ciprnode.ana-tatu.art".

The legend at the bottom for this scene is:

«Once the first ciprnode was created, every domain name owner can grab a copy, deploy it and get indexed in the Cipr.»

**Transition (45s to 47s)**:

Everything fades out except the Cipr cube that keeps growing and once reach the size of the scene, fades out too.

**Scene #6 (47s to 60s)**:

A final text appears in the center of the scene:

«Now, instead of complaining, you can just move on ;)»

**Transition (60s to 62s)**:

Everything fades out to black and that's all.
